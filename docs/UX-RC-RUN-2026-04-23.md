# UX RC Run - 2026-04-23

This run executes the release-candidate checklist against current `main` (`34ccfdc`) with automated evidence where possible and flags what still requires human visual sign-off in-browser.

## Target Scan Evidence

Source: `/tmp/ux_rc_scan_2026-04-23.json`

- `https://ek.co` -> `www.ek.co`, grade `B`, score `84`, status `200`, assessment limited `false`
- `https://bbc.co.uk` -> `www.bbc.co.uk`, grade `B`, score `88`, status `200`, assessment limited `false`
- `https://github.com` -> `github.com`, grade `A`, score `96`, status `200`, assessment limited `false`
- `https://wsj.com` -> `www.wsj.com`, grade `B`, score `84`, status `401`, assessment limited `true`
- `wsj.com` limitation message present:
  - title: `Assessment limited by an authenticated response`
  - detail: target required authentication before serving normal page

## Automated Checklist Outcome

### Passed (automated/code-verified)

- [x] Strengths vs Watch points copy pattern present in CT/IdP/WAF panels.
- [x] Monitoring trend logic covers:
  - hidden panel for zero snapshots without alerts
  - explicit message for one snapshot
  - sparkline + trend direction for two or more snapshots
- [x] Export headline policy is explicit in Markdown/HTML export code:
  - includes score/new/resolved change headline when diff exists
  - explicitly states category deltas are omitted by design
- [x] Assessment limitation behavior verified in live data (`wsj.com` returns limited assessment with explanatory text).
- [x] Priority/area logic regression guard present via unit tests.
- [x] Build and unit tests pass:
  - `npm run build`
  - `npm run test:app:unit`

### Requires human visual sign-off (in-browser)

- [ ] Tile alignment checks in top healthcheck block at desktop widths.
- [ ] Main visible risk width parity with analyst-read tile.
- [ ] Export button optical centering and equal-width feel across breakpoints.
- [ ] Posture Summary and Monitoring spacing/typography visual quality on:
  - `1280px`, `1024px`, `768px`, `390px`
- [ ] No clipping/overflow on long hostnames and long risk copy.
- [ ] Priority Actions visual hierarchy and readability on mixed/watch targets.

## Release Candidate Verdict

- Engineering/data/export logic: **ready**
- Visual polish sign-off: **pending final human pass**

Recommended next step: run the visual checklist in [UX-RELEASE-CANDIDATE-CHECKLIST.md](UX-RELEASE-CANDIDATE-CHECKLIST.md) in one browser session and convert remaining `[ ]` items to `[x]`.
